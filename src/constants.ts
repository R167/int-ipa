// A list of constants for the project

export const BASE_URL = process.env.PUBLIC_URL;

export const MANIFEST_FILE = `${BASE_URL}/manifest.yaml`;
export const TASK_FILE = `${BASE_URL}/manifest.yaml`;
